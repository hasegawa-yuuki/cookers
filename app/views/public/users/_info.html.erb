<div>
  <p>user name: <%= user.name %></p>
  <% if current_user == user %>
    <p><%= link_to "編集", edit_user_registration_path, class: "btn btn-warning" %></p>
    <p><%= link_to "削除", user_path(user), method: :delete, data: { confirm: "reary?" }, class: "btn btn-danger" %></p>
  <% end %>
  <p><%= link_to "新規投稿", new_post_path, class: "btn btn-primary" %></p>
</div>